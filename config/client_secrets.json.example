{
  "_comment": "EXAMPLE client_secrets.json (Desktop application). DO NOT use these values. Obtain real credentials from Google Cloud Console -> APIs & Services -> Credentials -> Create OAuth client ID (Desktop). Save your real file as config/client_secrets.json and keep it private.",
  "installed": {
    "client_id": "YOUR_CLIENT_ID.apps.googleusercontent.com",
    "project_id": "your-google-cloud-project-id",
    "auth_uri": "https://accounts.google.com/o/oauth2/auth",
    "token_uri": "https://oauth2.googleapis.com/token",
    "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
    "client_secret": "YOUR_CLIENT_SECRET",
    "redirect_uris": ["http://localhost:8080/"]
  }
}